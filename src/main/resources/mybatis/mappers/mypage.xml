<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mypage">


	<!-- 예매정보 상세 -->
	<select id="reserveDetail" resultType="ReserveDetailVo" parameterType="ReserveDetailVo">
<![CDATA[
select 
        u.user_no userNo,
        p.prod_no prodNo,
        u.name name,
        p.prodname prodname, 
        t.theatername theatername,
        h.hallname hallname,
        to_char(r.viewdate, 'yy-mm-dd hh24:mi') viewdate,
        r.rez_no rezNo,
        to_char(r.rezdate, 'yy-mm-dd hh24:mi') rezdate
        
from	reserve r, users u, product p, theater t, hall h
where	r.user_no= u.user_no
and     p.prod_no= r.prod_no
and     p.hall_no= h.hall_no
and     t.theater_no= h.theater_no
and     r.rez_no= #{rezNo}
and     u.user_no= #{userNo}
]]>
	</select>

	<!-- 예매 정보 상세 좌석 -->
	<select id="reserveSeatList" resultType="ReserveDetailVo" parameterType="ReserveDetailVo">
<![CDATA[
DELETE FROM	selseat
where	rez_no = #{rezNo}
and		selseat_no = #{selseatNo}
]]>
	</select>



</mapper>


<!-- 
예매상세 만들때 미흡사항 
1.	쿼리에 쓸모없는 userNo가 들어있었음 
2.	마이바티스 파라미터 타입 리저브디테일vo 
3.	sqlSession에 인자가 빠져있고 2개 이상의 매개 변수는 vo 나 map으로 처리해야하는데 처리 안되어있었음 
4.	리저브 디테일vo에 dcNo가 없지만, 선택좌석 가져올때 dcNo가 있는데 게터가 없어서 오류있었음. 

-->


<!-- 삭제없이 바꾸려면 alter table selseat drop CONSTRAINT FK_reserve_TO_selseat; alter table selseat add CONSTRAINT FK_reserve_TO_selseat foreign key (rez_no) REFERENCES reserve (rez_no) on 
	delete cascade; -->
	